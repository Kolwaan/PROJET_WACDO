# ========================================
# VARIABLES D'ENVIRONNEMENT - WACDO API
# ========================================
# Ce fichier sert de template.
# 1. Copie ce fichier et renomme-le en ".env"
# 2. Ne JAMAIS commiter le vrai fichier .env sur GitHub !

# === JWT Configuration ===
SECRET_KEY=votre_secret_key_super_securisee_ici
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# === Database Configuration ===
# En local (SQLite)
DATABASE_URL=sqlite:///./base_de_donnees.db

# En production (PostgreSQL - fourni automatiquement par Render)
# DATABASE_URL=postgresql://user:password@host:port/database

# === Application Configuration ===
APP_NAME=Backend FastAPI Wacdo
ENVIRONMENT=development
DEBUG=true

# === CORS Configuration ===
# En développement : autoriser tous les domaines
ALLOWED_ORIGINS=*

# En production : limiter aux domaines autorisés (séparés par des virgules)
# ALLOWED_ORIGINS=https://monapp.com,https://www.monapp.com

# === Security Configuration ===
ARGON2_TIME_COST=3
ARGON2_MEMORY_COST=65536
ARGON2_PARALLELISM=4
